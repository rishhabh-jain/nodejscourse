{
  "name": "thirdsection",
  "version": "1.0.0",
  "description": "useful commands:\r fs.writeFileSync // to create a file\r fs.appendFileSync('writefile.txt' , 'this is appended ') // to append\r fs.readFileSync('./notes.txt' , {encoding : 'utf-8'})",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0",
    "validator": "^13.1.1"
  }
}
